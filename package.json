{
  "name": "trustfactor-discord-bot",
  "version": "1.0.0",
  "description": "A community-driven Discord bot for tracking user scores through voting",
  "main": "index.js",
  "scripts": {
    "start:sharded": "node src/sharder.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "migrate": "echo Use Supabase CLI migrations instead (see README) && exit 0",
    "deploy-commands": "node src/utils/deploy-commands.js",
    "security:apply": "echo Use Supabase CLI (supabase db push) && exit 0",
    "security:verify": "echo Use Supabase dashboard policies && exit 0",
    "security:fix": "echo Use Supabase CLI && exit 0",
    "security:functions": "echo Functions secured via search_path in SQL && exit 0",
    "security:functions-verify": "echo Verify via Supabase dashboard && exit 0",
    "security:schema-verify": "echo Verify via Supabase CLI diff && exit 0",
    "security:full": "echo Use Supabase CLI + dashboard && exit 0",
    "performance:apply": "echo Use Supabase CLI migrations && exit 0",
    "performance:verify": "echo Use Supabase CLI diff && exit 0",
    "performance:schema-verify": "echo Use Supabase CLI diff && exit 0",
    "performance:optimize": "echo Use Supabase CLI && exit 0",
    "database:verify-all": "echo Use Supabase CLI && exit 0"
  },
  "keywords": [
    "discord",
    "bot",
    "scoring",
    "community",
    "voting"
  ],
  "author": "TrustFactor Bot Team",
  "license": "MIT",
  "dependencies": {
    "puppeteer": "^22.15.0",
    "discord.js": "^14.14.1",
    "@supabase/supabase-js": "^2.39.7",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
